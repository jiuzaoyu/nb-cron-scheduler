services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=${REDIS_HOST:-127.0.0.1}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - REDIS_DB=${REDIS_DB:-0}
    restart: unless-stopped
